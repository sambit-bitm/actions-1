name: My First Work flow

on: Push

jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
        - name: Welcome message
          run: echo "My 1st github actions job"
        - name: List files
          run: ls -ltr
        - name: Read file
          run: cat README.md